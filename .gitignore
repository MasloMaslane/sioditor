node_modules/
dist/
dist-types/
.vite/
*.tsbuildinfo
coverage/
.DS_Store
*.log

# Toolchain artifacts are built in CI and deployed alongside the app.
# They are never committed - see docs/toolchain.md.
apps/web/public/toolchain/
apps/web/public/pyodide/
toolchain/build/
toolchain/out/
test-results/
playwright-report/
# Fetched by toolchain/fetch-llvm.sh and fetch-wasi-sdk.sh into the repo, so that a local
# build and CI use identical paths. Multi-GB; never committed.
toolchain/llvm-project/
toolchain/wasi-sdk/
