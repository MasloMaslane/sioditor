# Both files must be served with these types or things fail in confusing ways:
#   .wasm as anything but application/wasm disables streaming compilation *and* Chrome's
#   wasm code cache, silently making every load slow.
types {
    application/wasm  wasm;
    application/zip   whl;
}
