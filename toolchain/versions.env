# Pinned once, consumed by every script and the CI workflow.
#
# LLVM must match the version inside wasi-sdk: the sysroot's libc++ headers and the
# compiler that parses them have to agree. wasi-sdk-34 carries LLVM 23.
WASI_SDK_VERSION=34.0
LLVM_TAG=llvmorg-23.1.0
